info:
  name: Get user by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/1"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

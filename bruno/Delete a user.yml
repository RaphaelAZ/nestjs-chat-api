info:
  name: Delete a user
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/users/2"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
